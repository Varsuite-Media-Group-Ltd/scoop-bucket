# Varsuite Scoop Bucket

Install Varsuite Mail on Windows:

```
scoop bucket add varsuite https://github.com/Varsuite-Media-Group-Ltd/scoop-bucket
scoop install varsuitemail
```

The manifest is published automatically by the Varsuite Mail release pipeline.
